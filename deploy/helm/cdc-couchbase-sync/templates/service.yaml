apiVersion: v1
kind: Service
metadata:
  name: {{ include "cdc-couchbase-sync.fullname" . }}
  labels:
    {{- include "cdc-couchbase-sync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "cdc-couchbase-sync.selectorLabels" . | nindent 4 }}
  ports:
    - name: metrics
      port: {{ .Values.service.metricsPort }}
      targetPort: metrics
      protocol: TCP
